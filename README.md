# 生命小秘书 — 演示静态站

这是为 `plan01` 文档生成的一个简单静态演示站点，包含项目概述、MVP、隐私架构和部署说明。

快速开始（PowerShell）：

```powershell
cd "e:\\tools\\bigengs\\plan01-site"
git init
git add .
git commit -m "Initial: Life Secretary demo site"
git branch -M main
# 替换为你在 GitHub 上新建仓库的 URL
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

推荐部署方式：
- 使用 GitHub Pages（仓库 `main` 分支 + `docs/` 或使用 Actions 部署）。
- 此目录已准备好直接推送并使用 Actions 部署到 Pages。

如需我帮你：我可以提交这些文件到当前工作区的 Git（本地提交）并给出后续推送步骤，或者我可以生成一个 `docs/` 版本并配置为直接用于 GitHub Pages。要我继续哪一步？
